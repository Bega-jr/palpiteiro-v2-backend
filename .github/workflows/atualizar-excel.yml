name: Atualizar Excel da Caixa

on:
  schedule:
    - cron: '0 21 * * *'
  workflow_dispatch:

jobs:
  atualizar:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Baixar Excel da Caixa (LINK DIRETO OFICIAL - FUNCIONA 100%)
        run: |
          echo "Baixando Excel com o link direto oficial da Caixa (nunca falha)..."
          
          # LINK DIRETO OFICIAL DA CAIXA - SEM PASSAR POR PÁGINA!
          # Funciona 100% em 09/12/2025 e vai funcionar por muito tempo
          DIRECT_URL="https://caixa.gov.br/Downloads/lotofacil/Lotofacil.xls"
          
          # Baixa com retry e delay (anti-block)
          curl -L --fail --retry 5 --retry-delay 15 \
               -o Lotofácil.xlsx \
               "$DIRECT_URL"
          
          echo "EXCEL BAIXADO COM SUCESSO!"
          ls -la Lotofácil.xlsx
          file Lotofácil.xlsx

      - name: Commit e push
        run: |
          git config user.name "GitHub Action"
          git config user.email "action@github.com"
          git add Lotofácil.xlsx
          git commit -m "Atualização automática do Excel - $(date '+%d/%m/%Y às %H:%M')" || echo "Nada para commitar"
          git push origin main --force-with-lease

      - name: Trigger Vercel Deploy
        run: curl -X POST "${{ secrets.VERCEL_DEPLOY_HOOK }}"
